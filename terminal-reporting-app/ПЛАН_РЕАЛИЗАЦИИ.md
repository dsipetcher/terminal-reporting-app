# План реализации системы учета грузов морского порта

## Обзор текущего состояния

### Что уже реализовано ✅
```
Backend (Express.js + Prisma + PostgreSQL):
├── API для вагонов (GET, POST, DELETE)
├── Модель Wagon (номер, груз, вес, склад, путь, время прибытия)
└── Модель Warehouse (номер, вместимость)

Frontend (React + TypeScript):
├── Страница учета вагонов
├── Страница учета складов
├── Базовая навигация (React Router)
└── Формы добавления данных
```

### Текущая архитектура
```
terminal-reporting-app/
├── backend/
│   ├── src/
│   │   └── index.ts (Express сервер)
│   ├── prisma/
│   │   └── schema.prisma (Модели БД)
│   └── package.json
└── frontend/
    ├── src/
    │   ├── App.tsx
    │   ├── pages/
    │   │   ├── WagonsPage.tsx
    │   │   └── WarehousesPage.tsx
    │   └── main.tsx
    └── package.json
```

---

## Стратегия реализации

### Принципы разработки
1. **Итеративное развитие** - от простого к сложному
2. **Не ломать работающее** - расширять, а не переписывать
3. **Модульность** - каждый новый функционал - отдельный модуль
4. **API-first** - сначала backend API, потом UI
5. **Тестирование** - покрытие критических модулей тестами

---

## ФАЗА 1: MVP - Базовая портовая система (3 месяца)

### Цель
Добавить учет морских судов и создать базовую функциональность портовой системы, используя существующую кодовую базу

---

### Месяц 1: Расширение базы данных и backend API

#### Неделя 1-2: Модели морских судов и судозаходов

**Задача 1.1: Обновление схемы Prisma**

```prisma
// Добавить в schema.prisma

// Морское судно
model Vessel {
  id            Int           @id @default(autoincrement())
  name          String        // Название судна
  imoNumber     String        @unique // IMO номер
  vesselType    VesselType    // Тип судна
  grossTonnage  Float?        // Валовая вместимость (GT)
  deadweight    Float?        // Дедвейт (тонн)
  length        Float?        // Длина (м)
  beam          Float?        // Ширина (м)
  draft         Float?        // Осадка (м)
  flag          String?       // Флаг государства
  owner         String?       // Владелец/судовая линия
  
  vesselCalls   VesselCall[]  // Связь с судозаходами
  
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

// Тип судна
enum VesselType {
  CONTAINER       // Контейнеровоз
  BULK_CARRIER    // Балкер
  TANKER          // Танкер
  RORO            // Ro-Ro
  GENERAL_CARGO   // Генеральных грузов
  OTHER           // Прочие
}

// Судозаход
model VesselCall {
  id            Int           @id @default(autoincrement())
  vessel        Vessel        @relation(fields: [vesselId], references: [id])
  vesselId      Int
  
  voyageNumber  String        // Рейсовый номер
  
  eta           DateTime      // Expected Time of Arrival
  etd           DateTime?     // Expected Time of Departure
  ata           DateTime?     // Actual Time of Arrival
  atd           DateTime?     // Actual Time of Departure
  
  berth         Berth?        @relation(fields: [berthId], references: [id])
  berthId       Int?          // Назначенный причал
  
  status        VesselCallStatus @default(EXPECTED)
  
  agent         String?       // Агент порта
  purpose       String?       // Цель захода
  
  containers    Container[]   // Контейнеры на судне
  
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

// Статус судозахода
enum VesselCallStatus {
  EXPECTED        // Ожидается
  ARRIVED         // Прибыло
  BERTHED         // У причала
  IN_OPERATION    // Обрабатывается
  DEPARTED        // Убыло
  CANCELLED       // Отменен
}

// Причал
model Berth {
  id            Int           @id @default(autoincrement())
  number        String        @unique // Номер причала
  name          String?       // Название
  berthType     BerthType     // Тип причала
  length        Float         // Длина причальной линии (м)
  depth         Float         // Глубина у причала (м)
  maxDeadweight Float?        // Макс. дедвейт судна
  
  isActive      Boolean       @default(true)
  
  vesselCalls   VesselCall[]  // Судозаходы
  
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

// Тип причала
enum BerthType {
  CONTAINER       // Контейнерный
  BULK            // Навалочных грузов
  LIQUID          // Наливной
  RORO            // Ro-Ro
  GENERAL         // Генеральных грузов
  MULTI_PURPOSE   // Многоцелевой
}

// Контейнер (базовая версия)
model Container {
  id              Int             @id @default(autoincrement())
  containerNumber String          @unique // Номер контейнера (11 символов)
  containerType   ContainerType   // Тип и размер
  
  status          ContainerStatus // Статус
  
  cargoDescription String?        // Описание груза
  grossWeight     Float?          // Вес брутто (тонн)
  
  vesselCall      VesselCall?     @relation(fields: [vesselCallId], references: [id])
  vesselCallId    Int?
  
  warehouse       Warehouse?      @relation(fields: [warehouseId], references: [id])
  warehouseId     Int?
  
  location        String?         // Текущее местоположение (блок-ряд-ярус)
  
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
}

// Тип контейнера
enum ContainerType {
  TWENTY_GP       // 20' General Purpose
  TWENTY_HC       // 20' High Cube
  FORTY_GP        // 40' General Purpose
  FORTY_HC        // 40' High Cube
  FORTY_FIVE_HC   // 45' High Cube
  TWENTY_RF       // 20' Refrigerated
  FORTY_RF        // 40' Refrigerated
  TWENTY_OT       // 20' Open Top
  FORTY_OT        // 40' Open Top
  TWENTY_FR       // 20' Flat Rack
  FORTY_FR        // 40' Flat Rack
  TWENTY_TK       // 20' Tank
}

// Статус контейнера
enum ContainerStatus {
  EMPTY           // Пустой
  FULL            // Полный
  ON_VESSEL       // На судне
  IN_TERMINAL     // На терминале
  ON_DELIVERY     // На доставке
  DELIVERED       // Доставлен
}

// Обновление модели Warehouse - добавить связь с контейнерами
model Warehouse {
  // ... существующие поля ...
  containers    Container[]     // Контейнеры на складе
}
```

**Действия**:
1. Обновить `backend/prisma/schema.prisma`
2. Создать миграцию: `npx prisma migrate dev --name add_vessels_and_berths`
3. Сгенерировать Prisma Client: `npx prisma generate`

---

**Задача 1.2: Backend API для судов**

Создать файл `backend/src/routes/vessels.ts`:

```typescript
import express from 'express';
import { PrismaClient } from '@prisma/client';

const router = express.Router();
const prisma = new PrismaClient();

// GET /api/vessels - Получить все суда
router.get('/', async (req, res) => {
  try {
    const vessels = await prisma.vessel.findMany({
      orderBy: { name: 'asc' },
    });
    res.json(vessels);
  } catch (error) {
    res.status(500).json({ error: 'Failed to fetch vessels' });
  }
});

// GET /api/vessels/:id - Получить судно по ID
router.get('/:id', async (req, res) => {
  try {
    const vessel = await prisma.vessel.findUnique({
      where: { id: Number(req.params.id) },
      include: {
        vesselCalls: {
          include: {
            berth: true,
          },
          orderBy: { eta: 'desc' },
          take: 10,
        },
      },
    });
    
    if (!vessel) {
      return res.status(404).json({ error: 'Vessel not found' });
    }
    
    res.json(vessel);
  } catch (error) {
    res.status(500).json({ error: 'Failed to fetch vessel' });
  }
});

// POST /api/vessels - Создать новое судно
router.post('/', async (req, res) => {
  try {
    const vessel = await prisma.vessel.create({
      data: req.body,
    });
    res.status(201).json(vessel);
  } catch (error) {
    res.status(400).json({ error: 'Failed to create vessel' });
  }
});

// PUT /api/vessels/:id - Обновить судно
router.put('/:id', async (req, res) => {
  try {
    const vessel = await prisma.vessel.update({
      where: { id: Number(req.params.id) },
      data: req.body,
    });
    res.json(vessel);
  } catch (error) {
    res.status(400).json({ error: 'Failed to update vessel' });
  }
});

// DELETE /api/vessels/:id - Удалить судно
router.delete('/:id', async (req, res) => {
  try {
    await prisma.vessel.delete({
      where: { id: Number(req.params.id) },
    });
    res.status(204).send();
  } catch (error) {
    res.status(400).json({ error: 'Failed to delete vessel' });
  }
});

export default router;
```

Создать файл `backend/src/routes/vesselCalls.ts`:

```typescript
import express from 'express';
import { PrismaClient } from '@prisma/client';

const router = express.Router();
const prisma = new PrismaClient();

// GET /api/vessel-calls - Получить все судозаходы
router.get('/', async (req, res) => {
  try {
    const { status, fromDate, toDate } = req.query;
    
    const where: any = {};
    
    if (status) {
      where.status = status;
    }
    
    if (fromDate || toDate) {
      where.eta = {};
      if (fromDate) where.eta.gte = new Date(fromDate as string);
      if (toDate) where.eta.lte = new Date(toDate as string);
    }
    
    const vesselCalls = await prisma.vesselCall.findMany({
      where,
      include: {
        vessel: true,
        berth: true,
        _count: {
          select: { containers: true },
        },
      },
      orderBy: { eta: 'asc' },
    });
    
    res.json(vesselCalls);
  } catch (error) {
    res.status(500).json({ error: 'Failed to fetch vessel calls' });
  }
});

// GET /api/vessel-calls/:id - Получить судозаход по ID
router.get('/:id', async (req, res) => {
  try {
    const vesselCall = await prisma.vesselCall.findUnique({
      where: { id: Number(req.params.id) },
      include: {
        vessel: true,
        berth: true,
        containers: true,
      },
    });
    
    if (!vesselCall) {
      return res.status(404).json({ error: 'Vessel call not found' });
    }
    
    res.json(vesselCall);
  } catch (error) {
    res.status(500).json({ error: 'Failed to fetch vessel call' });
  }
});

// POST /api/vessel-calls - Создать судозаход
router.post('/', async (req, res) => {
  try {
    const vesselCall = await prisma.vesselCall.create({
      data: req.body,
      include: {
        vessel: true,
        berth: true,
      },
    });
    res.status(201).json(vesselCall);
  } catch (error) {
    res.status(400).json({ error: 'Failed to create vessel call' });
  }
});

// PUT /api/vessel-calls/:id - Обновить судозаход
router.put('/:id', async (req, res) => {
  try {
    const vesselCall = await prisma.vesselCall.update({
      where: { id: Number(req.params.id) },
      data: req.body,
      include: {
        vessel: true,
        berth: true,
      },
    });
    res.json(vesselCall);
  } catch (error) {
    res.status(400).json({ error: 'Failed to update vessel call' });
  }
});

// PATCH /api/vessel-calls/:id/status - Обновить статус судозахода
router.patch('/:id/status', async (req, res) => {
  try {
    const { status } = req.body;
    const updateData: any = { status };
    
    // Автоматически устанавливать фактическое время
    if (status === 'ARRIVED' && !req.body.ata) {
      updateData.ata = new Date();
    }
    if (status === 'DEPARTED' && !req.body.atd) {
      updateData.atd = new Date();
    }
    
    const vesselCall = await prisma.vesselCall.update({
      where: { id: Number(req.params.id) },
      data: updateData,
      include: {
        vessel: true,
        berth: true,
      },
    });
    
    res.json(vesselCall);
  } catch (error) {
    res.status(400).json({ error: 'Failed to update vessel call status' });
  }
});

// DELETE /api/vessel-calls/:id - Удалить судозаход
router.delete('/:id', async (req, res) => {
  try {
    await prisma.vesselCall.delete({
      where: { id: Number(req.params.id) },
    });
    res.status(204).send();
  } catch (error) {
    res.status(400).json({ error: 'Failed to delete vessel call' });
  }
});

export default router;
```

**Обновить `backend/src/index.ts`**:

```typescript
import express from 'express';
import cors from 'cors';
import { PrismaClient } from '@prisma/client';

// Импорт маршрутов
import vesselRoutes from './routes/vessels';
import vesselCallRoutes from './routes/vesselCalls';
import berthRoutes from './routes/berths';
import containerRoutes from './routes/containers';

const app = express();
const prisma = new PrismaClient();

app.use(cors());
app.use(express.json());

// Существующие маршруты
app.get('/api/wagons', async (req, res) => {
  const wagons = await prisma.wagon.findMany();
  res.json(wagons);
});

app.post('/api/wagons', async (req, res) => {
  const { number, cargo, cargoWeight, warehouseId, track, arrivalAt } = req.body;
  const wagon = await prisma.wagon.create({
    data: { number, cargo, cargoWeight, warehouseId, track, arrivalAt },
  });
  res.json(wagon);
});

app.delete('/api/wagons/:id', async (req, res) => {
  const { id } = req.params;
  await prisma.wagon.delete({ where: { id: Number(id) } });
  res.status(204).send();
});

// Новые маршруты
app.use('/api/vessels', vesselRoutes);
app.use('/api/vessel-calls', vesselCallRoutes);
app.use('/api/berths', berthRoutes);
app.use('/api/containers', containerRoutes);

const PORT = 3001;
app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});
```

**Действия**:
1. Создать папку `backend/src/routes/`
2. Создать файлы маршрутов для vessels, vesselCalls, berths, containers
3. Обновить `index.ts`
4. Протестировать API через Postman/Insomnia

---

#### Неделя 3-4: Frontend для управления судами

**Задача 1.3: Страница управления причалами**

Создать файл `frontend/src/pages/BerthsPage.tsx`:

```typescript
import { useEffect, useState } from 'react';
import axios from 'axios';

interface Berth {
  id: number;
  number: string;
  name?: string;
  berthType: string;
  length: number;
  depth: number;
  maxDeadweight?: number;
  isActive: boolean;
}

export default function BerthsPage() {
  const [berths, setBerths] = useState<Berth[]>([]);
  const [form, setForm] = useState({
    number: '',
    name: '',
    berthType: 'CONTAINER',
    length: 0,
    depth: 0,
    maxDeadweight: 0,
  });

  const fetchBerths = async () => {
    const res = await axios.get('http://localhost:3001/api/berths');
    setBerths(res.data);
  };

  useEffect(() => {
    fetchBerths();
  }, []);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    
    await axios.post('http://localhost:3001/api/berths', {
      ...form,
      length: parseFloat(form.length.toString()),
      depth: parseFloat(form.depth.toString()),
      maxDeadweight: form.maxDeadweight ? parseFloat(form.maxDeadweight.toString()) : null,
    });
    
    setForm({
      number: '',
      name: '',
      berthType: 'CONTAINER',
      length: 0,
      depth: 0,
      maxDeadweight: 0,
    });
    
    fetchBerths();
  };

  const berthTypeLabels: Record<string, string> = {
    CONTAINER: 'Контейнерный',
    BULK: 'Навалочных грузов',
    LIQUID: 'Наливной',
    RORO: 'Ro-Ro',
    GENERAL: 'Генеральных грузов',
    MULTI_PURPOSE: 'Многоцелевой',
  };

  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold mb-6">Управление причалами</h1>

      <form onSubmit={handleSubmit} className="mb-8 bg-white p-6 rounded-lg shadow">
        <div className="grid grid-cols-2 gap-4">
          <input
            type="text"
            placeholder="Номер причала"
            value={form.number}
            onChange={(e) => setForm({ ...form, number: e.target.value })}
            className="border p-2 rounded"
            required
          />
          <input
            type="text"
            placeholder="Название (опционально)"
            value={form.name}
            onChange={(e) => setForm({ ...form, name: e.target.value })}
            className="border p-2 rounded"
          />
          <select
            value={form.berthType}
            onChange={(e) => setForm({ ...form, berthType: e.target.value })}
            className="border p-2 rounded"
          >
            {Object.entries(berthTypeLabels).map(([value, label]) => (
              <option key={value} value={value}>{label}</option>
            ))}
          </select>
          <input
            type="number"
            step="0.1"
            placeholder="Длина (м)"
            value={form.length || ''}
            onChange={(e) => setForm({ ...form, length: parseFloat(e.target.value) || 0 })}
            className="border p-2 rounded"
            required
          />
          <input
            type="number"
            step="0.1"
            placeholder="Глубина (м)"
            value={form.depth || ''}
            onChange={(e) => setForm({ ...form, depth: parseFloat(e.target.value) || 0 })}
            className="border p-2 rounded"
            required
          />
          <input
            type="number"
            step="100"
            placeholder="Макс. дедвейт (тонн)"
            value={form.maxDeadweight || ''}
            onChange={(e) => setForm({ ...form, maxDeadweight: parseFloat(e.target.value) || 0 })}
            className="border p-2 rounded"
          />
        </div>
        <button
          type="submit"
          className="mt-4 bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700"
        >
          Добавить причал
        </button>
      </form>

      <div className="bg-white rounded-lg shadow overflow-hidden">
        <table className="w-full">
          <thead className="bg-gray-50">
            <tr>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">№</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Название</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Тип</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Длина (м)</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Глубина (м)</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Макс. дедвейт (т)</th>
              <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Статус</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-gray-200">
            {berths.map((berth) => (
              <tr key={berth.id} className="hover:bg-gray-50">
                <td className="px-6 py-4 whitespace-nowrap">{berth.number}</td>
                <td className="px-6 py-4">{berth.name || '—'}</td>
                <td className="px-6 py-4">{berthTypeLabels[berth.berthType]}</td>
                <td className="px-6 py-4">{berth.length}</td>
                <td className="px-6 py-4">{berth.depth}</td>
                <td className="px-6 py-4">{berth.maxDeadweight || '—'}</td>
                <td className="px-6 py-4">
                  <span className={`px-2 py-1 text-xs rounded ${berth.isActive ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                    {berth.isActive ? 'Активен' : 'Неактивен'}
                  </span>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

**Задача 1.4: Страница судозаходов**

Создать файл `frontend/src/pages/VesselCallsPage.tsx`:

```typescript
import { useEffect, useState } from 'react';
import axios from 'axios';
import { format } from 'date-fns';
import { ru } from 'date-fns/locale';

interface Vessel {
  id: number;
  name: string;
  imoNumber: string;
  vesselType: string;
}

interface Berth {
  id: number;
  number: string;
  name?: string;
}

interface VesselCall {
  id: number;
  vessel: Vessel;
  voyageNumber: string;
  eta: string;
  etd?: string;
  ata?: string;
  atd?: string;
  berth?: Berth;
  status: string;
  agent?: string;
  _count?: {
    containers: number;
  };
}

export default function VesselCallsPage() {
  const [vesselCalls, setVesselCalls] = useState<VesselCall[]>([]);
  const [vessels, setVessels] = useState<Vessel[]>([]);
  const [berths, setBerths] = useState<Berth[]>([]);
  const [selectedStatus, setSelectedStatus] = useState<string>('ALL');

  const fetchData = async () => {
    const [callsRes, vesselsRes, berthsRes] = await Promise.all([
      axios.get('http://localhost:3001/api/vessel-calls'),
      axios.get('http://localhost:3001/api/vessels'),
      axios.get('http://localhost:3001/api/berths'),
    ]);
    
    setVesselCalls(callsRes.data);
    setVessels(vesselsRes.data);
    setBerths(berthsRes.data);
  };

  useEffect(() => {
    fetchData();
  }, []);

  const statusLabels: Record<string, string> = {
    EXPECTED: 'Ожидается',
    ARRIVED: 'Прибыло',
    BERTHED: 'У причала',
    IN_OPERATION: 'Обрабатывается',
    DEPARTED: 'Убыло',
    CANCELLED: 'Отменен',
  };

  const statusColors: Record<string, string> = {
    EXPECTED: 'bg-blue-100 text-blue-800',
    ARRIVED: 'bg-yellow-100 text-yellow-800',
    BERTHED: 'bg-purple-100 text-purple-800',
    IN_OPERATION: 'bg-orange-100 text-orange-800',
    DEPARTED: 'bg-gray-100 text-gray-800',
    CANCELLED: 'bg-red-100 text-red-800',
  };

  const updateStatus = async (id: number, newStatus: string) => {
    await axios.patch(`http://localhost:3001/api/vessel-calls/${id}/status`, {
      status: newStatus,
    });
    fetchData();
  };

  const filteredCalls = selectedStatus === 'ALL'
    ? vesselCalls
    : vesselCalls.filter(vc => vc.status === selectedStatus);

  return (
    <div className="p-6">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-3xl font-bold">Судозаходы</h1>
        <select
          value={selectedStatus}
          onChange={(e) => setSelectedStatus(e.target.value)}
          className="border p-2 rounded"
        >
          <option value="ALL">Все статусы</option>
          {Object.entries(statusLabels).map(([value, label]) => (
            <option key={value} value={value}>{label}</option>
          ))}
        </select>
      </div>

      <div className="grid gap-4">
        {filteredCalls.map((call) => (
          <div key={call.id} className="bg-white rounded-lg shadow p-6">
            <div className="flex justify-between items-start mb-4">
              <div>
                <h3 className="text-xl font-bold">{call.vessel.name}</h3>
                <p className="text-gray-600">IMO: {call.vessel.imoNumber} | Рейс: {call.voyageNumber}</p>
              </div>
              <span className={`px-3 py-1 rounded text-sm font-medium ${statusColors[call.status]}`}>
                {statusLabels[call.status]}
              </span>
            </div>

            <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4">
              <div>
                <p className="text-sm text-gray-500">Планируемое прибытие</p>
                <p className="font-medium">{format(new Date(call.eta), 'dd.MM.yyyy HH:mm', { locale: ru })}</p>
              </div>
              {call.ata && (
                <div>
                  <p className="text-sm text-gray-500">Фактическое прибытие</p>
                  <p className="font-medium">{format(new Date(call.ata), 'dd.MM.yyyy HH:mm', { locale: ru })}</p>
                </div>
              )}
              {call.berth && (
                <div>
                  <p className="text-sm text-gray-500">Причал</p>
                  <p className="font-medium">№{call.berth.number} {call.berth.name}</p>
                </div>
              )}
              {call._count && (
                <div>
                  <p className="text-sm text-gray-500">Контейнеров</p>
                  <p className="font-medium">{call._count.containers}</p>
                </div>
              )}
            </div>

            <div className="flex gap-2">
              <button
                onClick={() => updateStatus(call.id, 'ARRIVED')}
                disabled={call.status !== 'EXPECTED'}
                className="px-3 py-1 text-sm bg-blue-500 text-white rounded hover:bg-blue-600 disabled:opacity-50"
              >
                Прибыло
              </button>
              <button
                onClick={() => updateStatus(call.id, 'BERTHED')}
                disabled={call.status !== 'ARRIVED'}
                className="px-3 py-1 text-sm bg-purple-500 text-white rounded hover:bg-purple-600 disabled:opacity-50"
              >
                У причала
              </button>
              <button
                onClick={() => updateStatus(call.id, 'IN_OPERATION')}
                disabled={call.status !== 'BERTHED'}
                className="px-3 py-1 text-sm bg-orange-500 text-white rounded hover:bg-orange-600 disabled:opacity-50"
              >
                В обработке
              </button>
              <button
                onClick={() => updateStatus(call.id, 'DEPARTED')}
                disabled={call.status !== 'IN_OPERATION'}
                className="px-3 py-1 text-sm bg-gray-500 text-white rounded hover:bg-gray-600 disabled:opacity-50"
              >
                Убыло
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

**Обновить навигацию в `App.tsx`**:

```typescript
// Добавить в импорты
import BerthsPage from './pages/BerthsPage';
import VesselCallsPage from './pages/VesselCallsPage';

// Обновить меню в sidebar
<ul>
  <li><Link to="/vessel-calls">Судозаходы</Link></li>
  <li><Link to="/berths">Причалы</Link></li>
  <li><Link to="/wagons">Вагоны</Link></li>
  <li><Link to="/warehouses">Склады</Link></li>
</ul>

// Добавить маршруты
<Route path="/vessel-calls" element={<VesselCallsPage />} />
<Route path="/berths" element={<BerthsPage />} />
```

**Действия**:
1. Установить зависимости: `npm install date-fns`
2. Создать страницы BerthsPage и VesselCallsPage
3. Обновить App.tsx с новыми маршрутами
4. Добавить базовый CSS (Tailwind CSS или custom styles)

---

### Месяц 2: Контейнеры и автотранспорт

#### Неделя 5-6: Управление контейнерами

**Задача 2.1: API для контейнеров**
- CRUD операции для контейнеров
- Привязка к судозаходам и складам
- Отслеживание перемещений

**Задача 2.2: UI для контейнеров**
- Страница списка контейнеров с фильтрами
- Карточка контейнера с историей
- Форма приема/выдачи контейнера

#### Неделя 7-8: Автотранспорт и тайм-слоты

**Задача 2.3: Модель автотранспорта**

```prisma
model Truck {
  id              Int           @id @default(autoincrement())
  licensePlate    String        @unique
  truckType       TruckType
  carrier         String
  driverName      String?
  driverDocument  String?
  
  visits          TruckVisit[]
  
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
}

enum TruckType {
  TRUCK           // Фура
  CONTAINER_TRUCK // Контейнеровоз
  DUMP_TRUCK      // Самосвал
  REFRIGERATOR    // Рефрижератор
}

model TruckVisit {
  id              Int           @id @default(autoincrement())
  truck           Truck         @relation(fields: [truckId], references: [id])
  truckId         Int
  
  timeSlot        DateTime      // Забронированный тайм-слот
  timeIn          DateTime?     // Фактический въезд
  timeOut         DateTime?     // Фактический выезд
  
  purpose         String        // Цель визита
  gateNumber      String?       // Номер ворот
  
  status          TruckVisitStatus @default(SCHEDULED)
  
  container       Container?    @relation(fields: [containerId], references: [id])
  containerId     Int?
  
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
}

enum TruckVisitStatus {
  SCHEDULED       // Запланирован
  ARRIVED         // Прибыл
  IN_PROGRESS     // В процессе
  COMPLETED       // Завершен
  CANCELLED       // Отменен
}
```

**Задача 2.4: UI для автовизитов**
- Календарь тайм-слотов
- Форма заявки на въезд
- Отслеживание автомобилей на территории

---

### Месяц 3: Отчетность и доработки

#### Неделя 9-10: Базовая аналитика

**Задача 3.1: Dashboard с ключевыми метриками**
- Текущие судозаходы
- Загрузка причалов
- Заполненность складов
- Количество автовизитов за день

**Задача 3.2: Простые отчеты**
- Отчет о судозаходах за период
- Отчет о движении контейнеров
- Отчет о работе вагонов

#### Неделя 11-12: Система пользователей

**Задача 3.3: Аутентификация**
- Регистрация и вход
- JWT токены
- Защищенные маршруты

**Задача 3.4: Роли и права доступа**
- Администратор, диспетчер, оператор, клиент
- Разграничение доступа по ролям
- Логирование действий пользователей

---

## ФАЗА 2: Расширенный функционал (месяцы 4-6)

### Месяц 4: Планирование операций

**Задачи**:
- Планировщик погрузо-разгрузочных работ
- Управление перегрузочной техникой
- Оптимизация размещения грузов на складе
- Маршрутизация техники

### Месяц 5: Электронный документооборот

**Задачи**:
- Генерация PDF документов (накладные, акты)
- Шаблоны документов
- Электронная подпись
- Архив документов

### Месяц 6: Мобильное приложение для операторов

**Задачи**:
- React Native приложение
- Сканирование штрих-кодов/QR
- Подтверждение операций
- Offline режим

---

## ФАЗА 3: Интеграции (месяцы 7-9)

### Месяц 7: Интеграция с РЖД ЭТРАН

**Задачи**:
- API клиент для ЭТРАН
- Автоматический обмен данными о вагонах
- Синхронизация накладных
- Уведомления о прибытии составов

### Месяц 8: Web-портал для клиентов

**Задачи**:
- Отдельный фронтенд для клиентов
- Личный кабинет
- Отслеживание грузов
- Заявки на услуги
- Скачивание документов

### Месяц 9: Таможенная интеграция

**Задачи**:
- Формирование таможенных деклараций
- Интеграция с системой таможни
- Учет статусов таможенного оформления
- Уведомления о разрешениях

---

## ФАЗА 4: Аналитика и оптимизация (месяцы 10-12)

### Месяц 10: Расширенная аналитика

**Задачи**:
- BI Dashboard (Grafana/Metabase)
- KPI и показатели эффективности
- Графики и диаграммы
- Прогнозирование загрузки

### Месяц 11: Оптимизация и автоматизация

**Задачи**:
- Автоматическое планирование операций
- AI для прогнозирования ETA
- Оптимизация размещения грузов
- Автоматическое назначение техники

### Месяц 12: Внешние интеграции

**Задачи**:
- AIS/GPS трекинг судов
- RFID/ГЛОНАСС для транспорта
- Интеграция с весами
- API для сторонних систем

---

## Технологический стек

### Backend
```
├── Node.js + TypeScript
├── Express.js (REST API)
├── Prisma ORM
├── PostgreSQL (основная БД)
├── Redis (кэширование)
├── JWT (аутентификация)
└── Socket.io (real-time уведомления)
```

### Frontend
```
├── React + TypeScript
├── React Router v6
├── Axios (HTTP клиент)
├── TanStack Query (кэширование данных)
├── Tailwind CSS (стилизация)
├── Chart.js / Recharts (графики)
├── date-fns (работа с датами)
└── React Hook Form (формы)
```

### DevOps
```
├── Docker (контейнеризация)
├── GitHub Actions (CI/CD)
├── Nginx (reverse proxy)
└── PM2 (process manager)
```

---

## Организация кода (рекомендуемая структура)

```
terminal-reporting-app/
├── backend/
│   ├── src/
│   │   ├── routes/          # API маршруты
│   │   │   ├── vessels.ts
│   │   │   ├── vesselCalls.ts
│   │   │   ├── wagons.ts
│   │   │   ├── containers.ts
│   │   │   ├── trucks.ts
│   │   │   └── auth.ts
│   │   ├── controllers/     # Бизнес-логика
│   │   ├── services/        # Сервисный слой
│   │   ├── middleware/      # Express middleware
│   │   ├── utils/           # Утилиты
│   │   ├── types/           # TypeScript типы
│   │   └── index.ts
│   ├── prisma/
│   │   ├── schema.prisma
│   │   └── migrations/
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── pages/           # Страницы
│   │   │   ├── VesselCallsPage.tsx
│   │   │   ├── BerthsPage.tsx
│   │   │   ├── WagonsPage.tsx
│   │   │   ├── ContainersPage.tsx
│   │   │   └── DashboardPage.tsx
│   │   ├── components/      # Переиспользуемые компоненты
│   │   │   ├── VesselCard.tsx
│   │   │   ├── StatusBadge.tsx
│   │   │   └── DataTable.tsx
│   │   ├── hooks/           # Custom hooks
│   │   ├── api/             # API клиенты
│   │   ├── types/           # TypeScript типы
│   │   ├── utils/           # Утилиты
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── package.json
│
├── docs/                    # Документация
│   ├── ТРЕБОВАНИЯ.md
│   ├── ПЛАН_РЕАЛИЗАЦИИ.md
│   ├── API.md
│   └── DATABASE.md
│
└── README.md
```

---

## Чек-лист MVP (Фаза 1)

### База данных
- [ ] Модель Vessel (суда)
- [ ] Модель VesselCall (судозаходы)
- [ ] Модель Berth (причалы)
- [ ] Модель Container (контейнеры)
- [ ] Расширение модели Wagon
- [ ] Расширение модели Warehouse

### Backend API
- [ ] CRUD для судов
- [ ] CRUD для судозаходов
- [ ] CRUD для причалов
- [ ] CRUD для контейнеров
- [ ] API для обновления статусов
- [ ] Валидация данных

### Frontend
- [ ] Страница управления причалами
- [ ] Страница судозаходов
- [ ] Страница контейнеров
- [ ] Обновление навигации
- [ ] Responsive дизайн
- [ ] Базовая стилизация

### Безопасность
- [ ] Аутентификация пользователей
- [ ] JWT токены
- [ ] Защищенные маршруты
- [ ] Роли пользователей

### Документация
- [ ] README с инструкциями по запуску
- [ ] API документация
- [ ] Описание моделей БД
- [ ] Руководство пользователя

---

## Метрики успеха

### Технические метрики
- API response time < 200ms
- 99% uptime
- 0 критических багов в production
- Покрытие тестами > 70%

### Бизнес-метрики (после внедрения)
- Уменьшение времени обработки судна на 20%
- Уменьшение ошибок ввода данных на 80%
- Увеличение производительности операторов на 30%
- Сокращение простоев техники на 25%

---

## Риски и митигация

### Риск 1: Сложность интеграций
**Митигация**: Начать с mock данных, постепенно подключать реальные системы

### Риск 2: Недостаток знаний домена
**Митигация**: Регулярные консультации с портовыми операторами

### Риск 3: Технический долг
**Митигация**: Code review, регулярный рефакторинг, документирование

### Риск 4: Изменение требований
**Митигация**: Гибкая архитектура, модульный дизайн

---

## Следующие шаги

1. **Неделя 1**: Обновить схему БД, создать миграции
2. **Неделя 2**: Разработать backend API для судов
3. **Неделя 3**: Создать UI для управления причалами
4. **Неделя 4**: Создать UI для судозаходов
5. **Review**: Демо MVP, сбор обратной связи

---

*План составлен с учетом текущего состояния проекта и лучших практик разработки портовых систем управления*
